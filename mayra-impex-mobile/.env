# Public runtime config (safe to expose)

# Development: local backend on same LAN as Expo device/emulator
EXPO_PUBLIC_API_URL=http://10.207.205.159:5001/api

# Production/backup API (kept for quick switch when needed)
EXPO_PUBLIC_API_URL_RENDER=https://mayra-impel.onrender.com/api

# Do NOT add secrets here.
# Never store JWT secrets, DB URLs, or private API keys in mobile env/app bundle.
